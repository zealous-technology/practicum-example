name: Caller

on: workflow_call

jobs:
  CallProvider:
    runs-on: unbunto-latest
  
    steps:
      - name: "GetName"
        id: foo
        uses: actions/greet.yml@v1
      
      - name: "DisplayName"
        run: echo "The value is ${{ steps.foo.outputs.name }}"
