# This workflow is called from another workflow

name: SayHello

on:
  workflow_call:
    inputs:
      username:
        required: true
        type: string

jobs:
  reusable_job:

    runs-on: ubuntu-latest
    
    steps:
    - name: Echo Hello Message
      run: echo The usename is ${{ inputs.username }}
